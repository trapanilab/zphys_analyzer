# v36 File Tools local output

The `Show Loaded Metadata` and `Where Is Data Stored?` buttons now write their
output to a local read-only text box inside the File Tools tab instead of the
shared results area under the plot.
