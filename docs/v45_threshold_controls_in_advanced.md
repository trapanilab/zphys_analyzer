# v45 threshold controls moved to Advanced detection settings

The manual threshold input field and horizontal threshold slider were moved out
of the main Threshold detection panel and into the collapsible Advanced detection
settings area.

The main Threshold detection panel still shows the current threshold readout and
the plotted threshold line remains active/draggable.
