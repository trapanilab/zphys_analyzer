# v60 detect direction UI move

The Detect Direction dropdown was moved from the main Threshold detection panel
into the Advanced detection settings collapsible section. Detection behavior is
unchanged.
