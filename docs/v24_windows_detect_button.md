# v24 Windows tab detection button placement

Moved `Find Spikes / Events in Windows` to the top of the Windows tab, directly
under the explanatory text, so event detection can be run while fine-tuning
windows without scrolling.
