# v71 concatenate threshold update

Concatenate Selected Sweeps now updates the threshold value/line from the
concatenated display trace before the line is re-added to the plot.

This keeps the threshold line in a sensible location after the displayed trace
changes to the concatenated sweep.
