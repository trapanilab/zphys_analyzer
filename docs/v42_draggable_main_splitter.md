# v42 draggable main splitter

The main window now uses a horizontal splitter between:

- the left control panel
- the right plot/results area

The default left-panel width is compact, but the divider can be dragged to set
the proportion you want between controls and plot area.

The existing vertical splitter between the plot and results area remains.
